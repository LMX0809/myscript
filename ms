目的：查看一个文件夹中有多少个文件
====================================================
read -p "请输入要查询的文件夹的绝对路径: " pa
cd ${pa}
number=`ls | wc -l`        
echo "这个文件夹里有 $number 个文件"
====================================================
